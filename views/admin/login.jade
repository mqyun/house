extends ../layout

block css
  +usercss

block content
  +usercss
  #log_register
    .lr_box
      .tag
        ul
          li
            a.cur.top-tit(href='/login') 管理员登录
          .clear
      table.table_list
        tbody
          tr
            td
              input.text(name='account', type='text', placeholder='账号')
          tr
            td
              input.text(name='password', type='password', placeholder='密码')
          tr
            td
              input.submit.btn-adminlogin(type='button', value='登录')

block js
  script(type='text/javascript', src='/main/js/admin.js')
