extends layout

block css
  +usercss

block content
  +usercss
  #log_register
    .lr_box
      .tag
        ul
          li
            a.cur.top-tit(href='/login') 登录
          li
            a(href='/reg') 注册
          .clear
      table.table_list
        tbody
          tr
            td.btn-logintip(style='text-align: center;cursor: pointer;') 切换至用户登录 >>>
          tr
            td
              input.text(name='account', type='text', placeholder='账号')
          tr
            td
              input.text(name='password', type='password', placeholder='密码')
          tr
            td
              input.submit.btn-login(type='button', value='登录')
